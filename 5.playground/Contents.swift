import Foundation

/*
 let possibleNumber = "123"
let convertedNumber = Int(possibleNumber)

if let normalizedNumber = convertedNumber {
    print("converted valid number is \(normalizedNumber)")
}
*/



/*
let assumedString: String!
let isTrue = true

if isTrue {
    assumedString = "first value"
} else {
    assumedString = "second value"
}
*/



/*
 func minMaxArray(array:[Int]) -> (min: Int, max: Int) {
    var minValue = array[0]
    var maxValue = array[0]
    
    for element in array {
        if element < minValue {
            minValue = element
        } else if element > maxValue {
            maxValue = element
        }
    }
    return (minValue, maxValue)
}

print(minMaxArray(array: [-1, 2, 3, 4, 5]))
*/



/*
 func greetng(to person: String) -> String {
    "Hello" + person
}

greetng(to: "Harut")
*/



/*
 func arithmeticNum(values: Double...) -> Double {
    var countedNumber = 0.0
    
    for value in values {
        countedNumber += value
    }
    return countedNumber/Double(values.count)
}
arithmeticNum(values: 1,2,3)
*/




