import UIKit
import Foundation

/*
 Create a Student class, which has variables like:

name
age
gender
averageRating

Students should also have a class schedule for each working day.

If a student's average rating is greater than 4.5 then the student can get a grant.

Otherwise if a student's average rating is less than 2.3 he is expelled from university.
 */

/*
 class Student {
    let name: String
    let age: Int
    let gender: String
    let averageRating: Double
    
    init(name: String, age: Int, gender: String, averageRating: Double) {
        self.name = name
        self.age = age
        self.gender = gender
        self.averageRating = averageRating
    }
}
var Harut: Student = Student(name: "Harut", age: 18, gender: "Male", averageRating: 5.5)

for student in Student {
    if averageRating > 4.5 {
    print("Can get a grant")
    } else if averageRating < 2.3{
        print("Is expelled from University")
    }
}
 */



















